# Optional server-only token. Enables private contribution counts without exposing repository details.
GITHUB_SIGNAL_TOKEN=

# Optional server-only Strava credentials. Use either a short-lived access token
# or the OAuth client + refresh-token trio. Activity is reduced to weekly counts
# by type before the page is rendered; routes, locations, and measurements stay server-only.
STRAVA_ACCESS_TOKEN=
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=

# Keep false for local and preview deployments. Set true only for the
# production deployment after eliasb.dev has moved to this project.
SITE_INDEXABLE=false

# Public source identifiers and the expiring status live in
# src/content/integration-config.ts so production has useful defaults.
